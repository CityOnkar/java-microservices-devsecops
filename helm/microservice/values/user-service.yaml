name: user-service

image:
  repository: cityonkar/user-service
  tag: "git-6225dfb"
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 8080

replicaCount: 1

env:
  DB_HOST: user-service-db.cepumyous0um.us-east-1.rds.amazonaws.com
  DB_PORT: "3306"
  DB_USER: userdbuser


secrets:
  enabled: true
  name: user-db-secret
  data:
    DB_PASSWORD: "<7CB!_ka(WaKymU<"

ingress:
  enabled: true
  path: /users

serviceMonitor:
  enabled: true


resources:
  requests:
    cpu: "200m"
    memory: "512Mi"
  limits:
    cpu: "1000m"
    memory: "1024Mi"

probes:
  startup:
    initialDelaySeconds: 45
    failureThreshold: 30
  readiness:
    initialDelaySeconds: 20
  liveness:
    initialDelaySeconds: 60
