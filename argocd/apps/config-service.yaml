apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: config-service
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/CityOnkar/java-microservices-devsecops.git
    targetRevision: HEAD
    path: helm/microservice
    helm:
      valueFiles:
        - values/config-service.yaml

  destination:
    server: https://kubernetes.default.svc
    namespace: config-service

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
